{
  "//": "Node build for the server. Extends the root config but swaps the browser lib/types for Node, and follows imports into the shared src/sim + src/net. Run: npm run typecheck:server.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"],
    "noEmit": true
  },
  "include": ["./**/*.ts"]
}
